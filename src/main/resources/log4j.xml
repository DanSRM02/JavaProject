<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Appender para consola -->
    <appender name="Console" class="org.apache.log4j.ConsoleAppender">
        <!-- Formato del log -->
        <layout class="org.apache.log4j.PatternLayout">
            <param name="ConversionPattern" value="%d{ISO8601} %-5p [%t] %c{2} - %m%n"/>
        </layout>
    </appender>

    <!-- Nivel raíz de logs (puedes ajustar a DEBUG o INFO según prefieras) -->
    <root level="debug">
        <!-- Asocia el appender de consola al root logger -->
        <appender-ref ref="Console"/>
    </root>

    <!-- Logger específico para tu paquete o clase -->
    <logger name="com.oxi" level="debug">
        <appender-ref ref="Console"/>
    </logger>

</configuration>
